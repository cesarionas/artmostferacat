import { createClient } from "@/supabase/server";
import type { Person } from "@/types/database";
const decorate = async (supabase: Awaited<ReturnType<typeof createClient>>, row: any): Promise<Person> => { const images = (row.images ?? []).map((image: any) => ({ ...image, public_url: supabase.storage.from("people-images").getPublicUrl(image.path).data.publicUrl })); const files = await Promise.all((row.files ?? []).map(async (file: any) => ({ ...file, public_url: (await supabase.storage.from("people-files").createSignedUrl(file.path, 3600)).data.signedUrl }))); return { ...row, images, files, tags: row.people_tags?.map((x: any) => x.tags).filter(Boolean) } as Person; };
export async function getPeople(query = "", page = 0, pageSize = 12) { const supabase = await createClient(); let request = supabase.from("people").select("*, category:categories(*), images(*), files(*), people_tags(tags(*))", { count: "exact" }).eq("status", "published").order("created_at", { ascending: false }).range(page * pageSize, (page + 1) * pageSize - 1); if (query) request = request.or(`name.ilike.%${query}%,description.ilike.%${query}%`); const { data, count, error } = await request; if (error) throw error; return { people: await Promise.all((data ?? []).map(row => decorate(supabase, row))), count: count ?? 0 }; }
export async function getPerson(slug: string) { const supabase = await createClient(); const { data, error } = await supabase.from("people").select("*, category:categories(*), images(*), files(*), people_tags(tags(*))").eq("slug", slug).eq("status", "published").single(); return error ? null : decorate(supabase, data); }
