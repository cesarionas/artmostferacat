import { createClient } from "@/supabase/server";
import { formatBytes } from "@/lib/utils";
export default async function Dashboard() { const supabase = await createClient(); const [people, images, files] = await Promise.all([supabase.from("people").select("id", { count: "exact", head: true }), supabase.from("images").select("id", { count: "exact", head: true }), supabase.from("files").select("size")]); const size = files.data?.reduce((total, f) => total + f.size, 0) ?? 0; const stats = [["Pessoas", people.count ?? 0], ["Imagens", images.count ?? 0], ["Arquivos", files.data?.length ?? 0], ["Espaço usado", formatBytes(size)]]; return <><h1 className="text-3xl font-semibold">Visão geral</h1><p className="mt-2 text-foreground/60">Acompanhe o seu acervo.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([label, value]) => <div className="rounded-2xl border p-5" key={String(label)}><p className="text-sm text-foreground/60">{label}</p><p className="mt-2 text-3xl font-semibold">{value}</p></div>)}</div><div className="mt-6 rounded-2xl border p-5"><h2 className="font-semibold">Atividade recente</h2><p className="mt-3 text-sm text-foreground/60">Os uploads e alterações aparecerão aqui conforme o acervo crescer.</p></div></> }
