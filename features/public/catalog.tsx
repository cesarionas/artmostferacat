"use client";
import { useEffect, useState } from "react";
import { Search, SlidersHorizontal } from "lucide-react";
import type { Person } from "@/types/database";
import { PersonCard } from "./person-card";
export function Catalog({ initial }: { initial: Person[] }) { const [query, setQuery] = useState(""); const [people, setPeople] = useState(initial); const [loading, setLoading] = useState(false); useEffect(() => { const id = setTimeout(async () => { setLoading(true); const r = await fetch(`/api/people?q=${encodeURIComponent(query)}`); if (r.ok) setPeople((await r.json()).people); setLoading(false); }, 250); return () => clearTimeout(id); }, [query]); return <section className="mx-auto max-w-6xl px-4 py-8"><div className="mb-7 flex flex-col gap-3 sm:flex-row"><label className="flex h-12 flex-1 items-center gap-3 rounded-xl border bg-background px-4"><Search size={18}/><input aria-label="Pesquisar pessoas" className="w-full bg-transparent outline-none" value={query} onChange={e => setQuery(e.target.value)} placeholder="Pesquisar por nome ou descrição..."/></label><button className="flex h-12 items-center justify-center gap-2 rounded-xl border px-4 text-sm"><SlidersHorizontal size={17}/>Filtros</button></div>{loading ? <p className="py-16 text-center text-sm text-foreground/60">Buscando acervo...</p> : people.length ? <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{people.map(person => <PersonCard key={person.id} person={person}/>)}</div> : <div className="rounded-2xl border border-dashed py-20 text-center text-foreground/60">Nenhuma pessoa encontrada.</div>}</section> }
